#include <stdio.h>

int main()
{

    int j;

    printf("\n enter value n:");
    scanf("\n %d", &j);

    for (; j >= 1; j--)
    {
        printf("\n %d", j);
    }

    return 0;
}